import { ClassdirDirective } from './classdir.directive';

describe('ClassdirDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassdirDirective();
    expect(directive).toBeTruthy();
  });
});
