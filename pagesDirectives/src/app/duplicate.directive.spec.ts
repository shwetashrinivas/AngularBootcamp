import { DuplicateDirective } from './duplicate.directive';

describe('DuplicateDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateDirective();
    expect(directive).toBeTruthy();
  });
});
